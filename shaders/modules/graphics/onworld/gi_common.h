/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

const float DEFAULT_GI_SCALE = 0.8f;

float OW_GI_GetHemisphericFactor( in float3 aNormal, in float3 anUpVector )
{
	return saturate( dot(anUpVector, aNormal) * 0.5f + 0.5f );
}

float3 OW_GI_GetHemisphericAmbient( in float3 aGroundColor, in float3 aSkyColor, in float3 aNormal, in float3 anUpVector )
{
	return lerp( aGroundColor, aSkyColor, OW_GI_GetHemisphericFactor( aNormal, anUpVector ) );
}

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// EOF
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////